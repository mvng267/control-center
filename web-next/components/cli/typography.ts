/**
 * ReUI Typography System — 3 canonical sizes
 * Avoid arbitrary text-[Npx] — use these classes instead
 */

export const TEXT_CAPTION = 'text-[12px]';    // labels, badges, code, timestamps
export const TEXT_BODY = 'text-[14px]';       // chat, tool results, UI text
export const TEXT_HEADING = 'text-[16px]';    // headings (desktop), mobile via md:

export const TEXT_MONO = 'font-mono';         // code blocks, tool output

export const LEADING_RELAXED = 'leading-relaxed';  // multi-line text (chat, results)
export const LEADING_TIGHT = 'leading-tight';      // single-line (labels, badges)
